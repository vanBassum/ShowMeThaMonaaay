"""Backend package for ShowMeThaMonaaay.

The Flask runtime, engine, and supporting modules live here. This is the code
that gets frozen into the shipped single-file exe (frontend embedded separately).
Mutable runtime state (models, the user's correction log, sessions) lives outside
the install dir — see `paths`.
"""
