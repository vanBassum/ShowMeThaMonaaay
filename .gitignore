# cached item icons + hashes (regenerate with: python fetch_items.py && python build_hashes.py)
data/

# debug output, overlays, captures
out/
screenshots/
test screenshot*.png

# backend scan cache + saved corrections (repo-root only — leading slash so it does
# NOT also ignore UI folders like app/smtm-ui/src/components/sessions/)
/sessions/

# accumulated real-crop gallery (game-sourced training data; repo-root only)
/gallery/

# YOLO: training runs + downloaded base weights (data/yolo dataset covered by data/)
runs/
*.pt

# python
__pycache__/
*.pyc

# editor / tooling
.claude/

# kaggle upload staging (regenerate: python tools/pack_for_kaggle.py)

# all AI training scratch: base weights, dataset, runs, kaggle build
training/
