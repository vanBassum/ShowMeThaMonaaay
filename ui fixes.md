


So for generating test data from screenshots:
- First use chatgpt, or maybe claude to detect what should be on the screen.
- Next use template matching for the icons that where detected before to find 'best place'
- Show in the ui, for manual adjusting and approving.

Who even better, I can make some screenshots with some different rigs and backpacks. Then we do manual work of fitting a grid on them as perfectly as possible. Manually. Then we can use the generator to place random items in these grids and use that to train the detection model.

For the manual fitting of the grid, we can assume the gridsize. If we dont change resulolutiuon that wont matter and be static. We can probably automatically guess where the grid should be. Since the grid is also visible on empty backpacks. 

I wanna focus on backpack and rig anyway, since that is the most relevant for making max money per haul. The other gear is not something you chance during raid because of value. Its more, this is better, so i change it anyway. I dont need the marketplace value for that.



index.html needs:

- scroll zoom
- panning by draggin
- maybe autosnap to items with similar x or y values. (so columns and rows are easy to alling) when holding shift, disable this.
- 


